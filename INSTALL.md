# 🚀 ПОЛНАЯ ИНСТРУКЦИЯ ПО УСТАНОВКЕ И ЗАПУСКУ

## Этап 1: Подготовка окружения

### Шаг 1.1: Проверить Python

```bash
python --version
```

Нужен **Python 3.10 или выше**. Если версия ниже, скачайте с [python.org](https://www.python.org)

### Шаг 1.2: Проверить PostgreSQL

```bash
psql --version
```

Если не установлен:
- **Windows**: Скачайте с [postgresql.org](https://www.postgresql.org/download/windows/)
- **Mac**: `brew install postgresql`
- **Linux**: `sudo apt-get install postgresql`

### Шаг 1.3: Запустить PostgreSQL

**Windows:**
```bash
# PostgreSQL должен быть запущен как сервис
# Проверить: Services > postgresql
```

**Mac/Linux:**
```bash
pg_ctl -D /usr/local/var/postgres start
# или
brew services start postgresql
```

---

## Этап 2: Клонирование и настройка проекта

### Шаг 2.1: Перейти в папку проекта

```bash
cd "c:\Users\User\Desktop\New folder\BuildFlow"
```

### Шаг 2.2: Создать виртуальное окружение

```bash
# Windows
python -m venv venv

# Mac/Linux
python3 -m venv venv
```

### Шаг 2.3: Активировать окружение

**Windows:**
```bash
venv\Scripts\activate
```

**Mac/Linux:**
```bash
source venv/bin/activate
```

### Шаг 2.4: Обновить pip

```bash
python -m pip install --upgrade pip
```

### Шаг 2.5: Установить зависимости

```bash
pip install -r requirements.txt
```

Это займёт 1-2 минуты.

---

## Этап 3: Конфигурация БД

### Шаг 3.1: Создать БД в PostgreSQL

```bash
# Подключиться к PostgreSQL
psql -U postgres

# Создать БД
CREATE DATABASE buildflow;

# Выход
\q
```

### Шаг 3.2: Обновить .env файл

Откройте файл `.env` в корне проекта и обновите:

```dotenv
BOT_TOKEN=8537417008:AAEPq10iZJwfDI8UC2O2IUGKFGpARdII08Q
DATABASE_URL=postgresql://postgres:password@localhost:5432/buildflow
ENV=dev
```

**Замените:**
- `password` - ваш пароль PostgreSQL
- `localhost` - адрес сервера (обычно localhost)
- `buildflow` - имя БД

---

## Этап 4: Запуск бота

### Шаг 4.1: Убедиться что окружение активировано

```bash
# На Windows вы должны видеть (venv) в начале строки
(venv) C:\Users\User\Desktop\New folder\BuildFlow>
```

### Шаг 4.2: Запустить бота

```bash
python main.py
```

### Шаг 4.3: Ожидаемый результат

```
[INFO] Starting BuildFlow bot
[INFO] Database tables created or verified
```

Если это видите - **бот успешно запущен!** ✅

---

## Этап 5: Тестирование в Telegram

### Шаг 5.1: Найти бота

1. Откройте Telegram
2. Найдите вашего бота (имя из BotFather)
3. Отправьте: `/start`

### Шаг 5.2: Выбрать роль

1. Выберите: **👷 Прораб** или **👤 Заказчик**
2. Вы попадёте в главное меню

### Шаг 5.3: Тест функций

**Для Прораба:**
1. ➕ Создать проект
   - Название: "Тестовый проект"
   - Адрес: "г. Минск, ул. Ленина"
   - Бюджет: 50000
2. 💰 Добавить расход
   - Выбрать проект
   - Сумма: 1250.50
   - Категория: Материалы
3. 📸 Фото отчёт
   - Загрузить любую фотографию
4. 📊 Отчёт
   - Увидеть План vs Факт

**Для Заказчика:**
- Может просмотреть проекты и отчёты
- Не может создавать проекты

---

## Возможные проблемы и решения

### ❌ `BOT_TOKEN is not set`

**Проблема:** Токен не найден в .env

**Решение:**
```bash
# Проверить .env файл
cat .env

# BOT_TOKEN должен быть там
# Если не там - добавьте вручную
```

### ❌ `Database connection error`

**Проблема:** Не удаётся подключиться к PostgreSQL

**Решение:**
```bash
# Проверить что PostgreSQL запущен
# Windows: Services > postgresql должен быть запущен
# Mac: brew services list
# Linux: sudo systemctl status postgresql

# Проверить DATABASE_URL в .env
# По умолчанию:
postgresql://postgres:password@localhost:5432/buildflow
```

### ❌ `ModuleNotFoundError: No module named 'aiogram'`

**Проблема:** Зависимости не установлены

**Решение:**
```bash
# Убедиться что окружение активировано
# Затем:
pip install -r requirements.txt
```

### ❌ `Exception: relation "users" does not exist`

**Проблема:** Таблицы не созданы в БД

**Решение:**
Бот должен создать таблицы автоматически при первом запуске. Если не создал:
```bash
# Удалить БД
dropdb buildflow

# Пересоздать
createdb buildflow

# Запустить бота ещё раз
python main.py
```

---

## Команды для отладки

### Проверить Python окружение

```bash
python -c "import sys; print(sys.version)"
```

### Проверить установленные пакеты

```bash
pip list | grep -E "aiogram|sqlalchemy|psycopg2|python-dotenv"
```

### Проверить подключение к БД

```bash
psql -U postgres -c "SELECT 1"
```

### Протестировать импорты

```bash
python -c "import aiogram; import sqlalchemy; print('✓ All imports OK')"
```

---

## Запуск в фоне (дополнительно)

### Windows (PowerShell)

```bash
# Запустить в фоне
Start-Process python "main.py"

# Или в новом окне
Start-Process powershell -ArgumentList "cd $PWD; python main.py"
```

### Linux/Mac

```bash
# Запустить в фоне
python main.py &

# Или с nohup (сохранится после закрытия терминала)
nohup python main.py > bot.log &
```

---

## Остановка бота

**Ctrl + C** в терминале

или

```bash
# Найти процесс
ps aux | grep main.py

# Завершить (замените PID)
kill <PID>
```

---

## Следующие шаги

1. **Прочитайте README.md** - полное описание функций
2. **Посмотрите REPORT.txt** - детальный отчёт о реализации
3. **Изучите bot/handlers/base.py** - логика бота
4. **Расширяйте функции** - добавляйте свои команды

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи в консоли
2. Убедитесь что все требования из пункта 1 выполнены
3. Посмотрите FAQ в README.md

---

**Готово! Ваш бот должен работать!** 🎉

```bash
python main.py
```

И начните создавать проекты в Telegram! 🚀
